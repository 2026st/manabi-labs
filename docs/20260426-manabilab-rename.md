# サイト名文言変更（まなびログ → まなびラボ）

## 変更内容

- `index.html` のタイトル・OGP・Xカードの文言を `まなびラボ` に変更
- `app/components/Header.tsx` のヘッダー表示名と `aria-label` を `まなびラボ` に変更

## 実装意図

- UI表示名とメタ情報を同一名称に統一し、ブランド名の不一致を防ぐ

## 具体例

- 変更前: ブラウザタブ表示が `まなびログ`
- 変更後: ブラウザタブ表示が `まなびラボ`
